/**
 * Converts .fragment.glsl and vertex.glsl file pairs into .shader.ts files which export a ShaderProgram object.
 * @param path - The path to the .glsl files to convert.
 */
export function convertShaders(path: string) {
    console.log("********** convertShaders: " + path);
}
